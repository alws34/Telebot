namespace Telebot.Common
{
    public enum OSVersion
    {
        Win10 = 100, // 10.0
        Win8 = 62, // 6.2
        Win7 = 61, // 6.1
        Vista = 60, // 6.0
        XP = 52, // 5.2 64Bit
        Win2k = 50, // 5.0
        All = 0 // compatible with any os
    }
}
